<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.mapper.BlobTableMapper">
	<select id="select" resultType="mybatis.BlobTable">
		SELECT key, image FROM blob_table
		WHERE key = #{key}
	</select>
	<insert id="insert" parameterType="mybatis.BlobTable"
		statementType="PREPARED">
		INSERT INTO blob_table(key, image)
		VALUES(#{key},　#{image})
	</insert>
</mapper>
